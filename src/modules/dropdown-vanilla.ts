import dropdown from '../components/atoms/DropdownVanilla/lib/toggle-dropdown';

document.addEventListener('DOMContentLoaded', () => {
  dropdown();
});

document.addEventListener('initDropdown', () => {
  dropdown();
});
