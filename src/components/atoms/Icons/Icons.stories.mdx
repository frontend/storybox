import { Meta, Preview } from '@storybook/addon-docs/blocks';

import Icon from '../Icon';

import Icons, { iconList } from './Icons';

<Meta title="Docs/Icons" />

# Icons

<Preview>
  <Icons />
    <table className="table-auto">
      <tbody>
        {Object.keys(iconList).map(icon => (
          <tr key={`icon-listed-${icon}`}>
            <td className="px-4 py-2 border border-gray-100">
              <code>{icon}</code>
            </td>
            <td className="px-4 py-2 text-3xl border border-gray-100">
              <Icon name={icon} />
            </td>
          </tr>
        ))}
      </tbody>
    </table>
</Preview>